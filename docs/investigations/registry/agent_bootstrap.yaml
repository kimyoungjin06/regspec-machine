version: 1
updated_at: 2026-02-26
module: 03_regspec_machine
purpose: "최소 컨텍스트로 현재 active stream 실행/검증 시작 (active lock 기반)"

single_source_of_truth:
  - modules/03_regspec_machine/docs/investigations/registry/active_stream_lock.yaml

read_order:
  - AGENTS.md
  - modules/03_regspec_machine/docs/investigations/registry/active_stream_lock.yaml
  - modules/03_regspec_machine/docs/investigations/registry/project_flow_branching.md
  - "<active_stream_paths.ongoing_doc>"
  - modules/03_regspec_machine/docs/investigations/registry/stream_registry.md

first_10min:
  command_policy:
    must_run: "1~3 commands"
    must_check: "1~3 artifacts"
    optional: "0+ commands/artifacts"
  resolve_from_lock:
    commands: "<bootstrap.must_run + bootstrap.optional>"
    success_artifacts: "<bootstrap.must_check>"

guardrails:
  - "active stream은 1개만 유지한다."
  - "신규 실험은 active stream의 ongoing.md에만 기록한다."
  - "legacy 문서는 조회 전용으로 유지한다."
